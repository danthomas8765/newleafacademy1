---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';

// 1. Import the Image component AND your headshot images
import { Image } from 'astro:assets';
import danThomasImage from '../assets/danthomas.jpg';
import kaiMcKimImage from '../assets/kaimckim.jpg';
import marcusReesImage from '../assets/marcusrees.jpg';
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title="Meet Our Tutors | New Leaf Academy" description="Meet the experienced and specialist tutors at New Leaf Academy." />
	</head>
	<body>
		<Header />
		<main>
			<h1>Meet Our Tutors</h1>
			<p>All of us are passionate about teaching and have the experience to adapt to each student's needs to create the perfect environment, fostering learning and confidence.</p>

			<a href="/dan-thomas/" class="tutor-card-link">
				<div class="tutor-card">
					<Image
						src={danThomasImage}  
						alt="Dan Thomas Headshot"
						class="tutor-headshot"
						width={150}
						height={150}
						format="webp"
					/>
					<div class="tutor-info">
						<h2>Dan Thomas</h2>
						<p>Specialising in <strong>mathematics</strong> and <strong>science</strong> for GCSE and A level <strong>physics</strong> and <strong>mathematics</strong>.</p>
					</div>
					<span class="more-info-link">Click here for more →</span>
				</div>
			</a>

			<a href="/kai-mckim/" class="tutor-card-link">
				<div class="tutor-card">
					<Image
						src={kaiMcKimImage}  
						alt="Kai McKim Headshot"
						class="tutor-headshot"
						width={150}
						height={150}
						format="webp"
					/>
					<div class="tutor-info">
						<h2>Kai McKim</h2>
						<p>Specialising in <strong>English literature</strong> and <strong>language</strong> for GCSE and A level.</p>
					</div>
					<span class="more-info-link">Click here for more →</span>
				</div>
			</a>

			<a href="/marcus-rees/" class="tutor-card-link">
				<div class="tutor-card">
					<Image
						src={marcusReesImage} 
						alt="Marcus Rees Headshot"
						class="tutor-headshot"
						width={150}
						height={150}
						format="webp"
					/>
					<div class="tutor-info">
						<h2>Marcus Rees</h2>
						<p>Specialising in <strong>mathematics</strong>, <strong>further mathematics</strong>, <strong>business studies</strong> and <strong>science</strong> for GCSE and A level <strong>mathematics</strong> and <strong>economics</strong>.</p>
					</div>
					<span class="more-info-link">Click here for more →</span>
				</div>
			</a>
			<div class="cta-section">
				<h2>Still Need Help?</h2>
				<p>Our expert tutors are ready to provide the one-to-one support your child needs. Book a free consultation to get started today!</p>
				<a href="/consultations" class="cta-button">
					Book Your Free Consultation and Taster Session
				</a>
			</div>
		</main>
		<Footer />
	</body>
</html>

<style>

    :root {
		--primary-color: var(--accent);
		--text-color: var(--accent);
		--card-bg-color: #f9f9f9;
        --border-color: #ddd;
        --box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
	}

	body {
		font-family: sans-serif;
		line-height: 1.6;
		color: var(--accent-light);
		margin: 0;
		padding: 0;
	}

	main {
		max-width: 800px;
		margin: 2em auto;
		padding: 0 1em;
		box-sizing: border-box;
		color: var(--accent-light);
	}

	h1 {
		color: var(--accent-light);
		font-size: clamp(2em, 5vw, 2.5em);
		text-align: center;
		margin-bottom: 0.5em;
	}
	
	p {
		font-size: clamp(1em, 2vw, 1.1em);
		color: #f9f9f9;;
	}

	.tutor-card-link {
		text-decoration: none;
		color: inherit;
		display: block;
		margin: 2em 0;
	}

	.tutor-card {
		display: flex;
		align-items: center;
		gap: 20px;
		padding: 1.5em;
		background-color: var(--card-bg-color);
		border: 1px solid var(--border-color);
		border-radius: 10px;
		box-shadow: var(--box-shadow);
		transition: transform 0.3s ease, box-shadow 0.3s ease;
        position: relative;
	}

	.tutor-card-link:hover .tutor-card {
		transform: translateY(-5px);
		box-shadow: 0 8px 15px rgba(0, 0, 0, 0.15);
	}
	
	.tutor-headshot {
		width: clamp(100px, 25vw, 150px);
		height: clamp(100px, 25vw, 150px);
		object-fit: cover;
		border-radius: 50%;
		border: 4px solid var(--accent);
		flex-shrink: 0;
	}
	
	.tutor-info {
		flex-grow: 1;
	}
	
	.tutor-info h2 {
		color: var(--accent);
		margin: 0 0 0.5em 0;
		font-size: clamp(1.5em, 4vw, 2em);
		text-decoration: none;
	}
	
	.tutor-info p {
		margin: 0;
		color: var(--accent);
		word-break: break-word; /* Force long words to wrap */
	}

    .more-info-link {
        position: absolute;
        top: 1.5em; /* Position to top */
        right: 1.5em; /* Position to right */
        font-size: 0.8em;
        color: var(--accent);
        transition: color 0.3s ease;
    }

    .tutor-card-link:hover .more-info-link {
        color: var(--accent-pop);
    }

    .cta-section {
		text-align: center;
		margin-top: 4em;
		padding: 2.5em;
		background-color: white;
		border-radius: 12px;
		box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
		color: var(--accent);
	}
	
	.cta-section h2 {
		font-size: clamp(1.5em, 4vw, 1.8em);
		margin-top: 0;
		color: var(--accent);
	}
	.cta-section p {
		margin-bottom: 1.5em;
		color: var(--accent)
	}

	.cta-button {
		display: inline-block;
		padding: 1em 2em;
		background-color: var(--accent-pop);
		color: white;
		font-weight: bold;
		text-decoration: none;
		border-radius: 8px;
		box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
		transition: transform 0.3s ease, box-shadow 0.3s ease;
	}

	.cta-button:hover {
		transform: translateY(-5px);
		box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
	}
	
	/* --- Media Query for Mobile/Tablet Optimization --- */
	@media (max-width: 650px) {
		 .tutor-card {
        flex-direction: column;
        text-align: center;
        gap: 15px;
        padding-bottom: 3.5em;
    }

    .tutor-headshot {
        margin-bottom: 0.5em;
    }

    .tutor-info {
        padding-bottom: 0;
    }

    .more-info-link {
        position: absolute;
        left: 50%;
        transform: translateX(-50%);
        bottom: 1em;
        top: auto; /* <-- Add this line */
        width: fit-content;
    }
	}
</style>